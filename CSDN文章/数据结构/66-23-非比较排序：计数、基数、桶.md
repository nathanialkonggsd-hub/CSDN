@[TOC](C语言数据结构系列：非比较排序篇)

# C语言数据结构系列（二十三）：非比较排序——计数、基数、桶

> 🎯 **本篇目标**：掌握三种非比较排序算法！

---

## 一、前言

哈喽小伙伴们！👋

今天我们来学习三种**不基于比较**的排序，可以突破O(nlogn)下限！

---

## 二、计数排序

### 2.1 思想

> 💡 统计每个值出现的次数，按顺序输出

### 2.2 代码实现

```c
void countingSort(int arr[], int n) {
    int max = arr[0], min = arr[0];
    for (int i = 1; i < n; i++) {
        if (arr[i] > max) max = arr[i];
        if (arr[i] < min) min = arr[i];
    }
    
    int range = max - min + 1;
    int *count = (int*)calloc(range, sizeof(int));
    
    for (int i = 0; i < n; i++) {
        count[arr[i] - min]++;
    }
    
    int index = 0;
    for (int i = 0; i < range; i++) {
        while (count[i] > 0) {
            arr[index++] = i + min;
            count[i]--;
        }
    }
    free(count);
}
```

**时间：O(n+k)，空间：O(k)，稳定**

---

## 三、桶排序

### 3.1 思想

> 💡 将数据分到多个桶，每个桶单独排序，最后合并

### 3.2 代码实现

```c
void bucketSort(int arr[], int n) {
    int max = arr[0], min = arr[0];
    for (int i = 1; i < n; i++) {
        if (arr[i] > max) max = arr[i];
        if (arr[i] < min) min = arr[i];
    }
    
    int bucketCount = (max - min) / n + 1;
    // 简化：使用链表实现桶
    // 这里省略具体实现
}
```

**时间：O(n+k)，稳定**

---

## 四、基数排序

### 4.1 思想

> 💡 按位排序，从个位到最高位，使用计数排序作为子排序

### 4.2 代码实现

```c
void radixSort(int arr[], int n) {
    int max = arr[0];
    for (int i = 1; i < n; i++)
        if (arr[i] > max) max = arr[i];
    
    for (int exp = 1; max / exp > 0; exp *= 10) {
        int output[n], count[10] = {0};
        
        for (int i = 0; i < n; i++)
            count[(arr[i] / exp) % 10]++;
        
        for (int i = 1; i < 10; i++)
            count[i] += count[i - 1];
        
        for (int i = n - 1; i >= 0; i--) {
            output[count[(arr[i] / exp) % 10] - 1] = arr[i];
            count[(arr[i] / exp) % 10]--;
        }
        
        for (int i = 0; i < n; i++)
            arr[i] = output[i];
    }
}
```

**时间：O(d*(n+k))，稳定**

---

## 五、复杂度对比

| 算法 | 时间 | 空间 | 稳定 |
|:----:|:----:|:----:|:----:|
| 计数 | O(n+k) | O(k) | ✅ |
| 桶 | O(n+k) | O(n+k) | ✅ |
| 基数 | O(d*(n+k)) | O(n+k) | ✅ |

---

## 六、排序算法总结

| 算法 | 平均 | 最好 | 最坏 | 空间 | 稳定 |
|:----:|:----:|:----:|:----:|:----:|:----:|
| 冒泡 | O(n²) | O(n) | O(n²) | O(1) | ✅ |
| 选择 | O(n²) | O(n²) | O(n²) | O(1) | ❌ |
| 插入 | O(n²) | O(n) | O(n²) | O(1) | ✅ |
| 希尔 | O(n^1.3) | - | O(n²) | O(1) | ❌ |
| 快排 | O(nlogn) | O(nlogn) | O(n²) | O(logn) | ❌ |
| 归并 | O(nlogn) | O(nlogn) | O(nlogn) | O(n) | ✅ |
| 堆排 | O(nlogn) | O(nlogn) | O(nlogn) | O(1) | ❌ |

---

## 七、下篇预告

下一篇我们将学习 **查找算法：从顺序到二分**！

---

> 💡 非比较排序可以突破O(nlogn)，但有局限性！👍
